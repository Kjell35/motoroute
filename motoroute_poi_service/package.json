{
  "name": "motoroute-poi-backend",
  "version": "1.0.0",
  "description": "MotoRoute Biker-POI Backend: täglicher OSM-Scan + Realtime-Sync",
  "main": "src/server.js",
  "engines": {
    "node": ">=18"
  },
  "scripts": {
    "start": "node src/server.js",
    "scan:now": "node -e \"require('./src/dailyPoiScan').runDailyScan().then(()=>process.exit(0))\"",
    "test": "jest"
  },
  "dependencies": {
    "dotenv": "^16.4.5",
    "express": "^4.19.2",
    "node-cron": "^3.0.3",
    "pg": "^8.12.0",
    "socket.io": "^4.7.5"
  },
  "devDependencies": {
    "jest": "^29.7.0"
  }
}
