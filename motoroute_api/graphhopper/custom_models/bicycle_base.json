{
  "//": "Base access/speed rules for the bicycle vehicle profile. Motorways/trunk roads are excluded outright (multiply_by 0), not just de-weighted, since they are normally illegal for bicycles - this must be verified against local traffic law data per region before any non-DE rollout.",
  "speed": [
    { "if": "road_class == SECONDARY || road_class == TERTIARY", "limit_to": "25" },
    { "if": "road_class == RESIDENTIAL || road_class == LIVING_STREET", "limit_to": "18" },
    { "if": "road_class == CYCLEWAY", "limit_to": "20" },
    { "if": "road_class == TRACK", "limit_to": "12" }
  ],
  "priority": [
    { "if": "road_class == MOTORWAY || road_class == TRUNK", "multiply_by": "0" },
    { "if": "road_class == CYCLEWAY", "multiply_by": "1.5" },
    { "if": "!bike_access", "multiply_by": "0" }
  ]
}
