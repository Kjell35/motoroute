{
  "//": "Fahrstil: Extra kurvig. Aggressively seeks curvature even at a real time cost - this profile is expected to produce noticeably longer ETAs than 'Kurvig' for the same destination, by design. Motorways/trunk roads are almost fully excluded rather than just de-weighted.",
  "priority": [
    { "if": "curvature > 0.10", "multiply_by": "1.5" },
    { "if": "curvature > 0.25", "multiply_by": "2.2" },
    { "if": "curvature > 0.5", "multiply_by": "3.0" },
    { "if": "road_class == MOTORWAY", "multiply_by": "0.1" },
    { "if": "road_class == TRUNK", "multiply_by": "0.3" }
  ]
}
